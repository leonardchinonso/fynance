# Parse phase-timing calibration

Generated by `node scripts/calibrate_parse.mjs` against a local backend with a real
Anthropic key. 6 configs × 6 runs each, first (warm-up) run dropped. Raw means in
`parse_timings.json`. Durations in ms.

| config | total | pre (start→llm_start) | llm (llm_start→post) | post (→done) | llm frac |
|---|---|---|---|---|---|
| txn-small (monzo.csv) | 2236 ±301 | 775 | 1460 | 1 | 0.65 |
| txn-mid (lloyds.csv) | 2352 ±847 | 1011 | 1340 | 1 | 0.57 |
| txn-multi (3 CSVs) | 5036 ±3571 | 599 | 4436 | 2 | 0.88 |
| pdf (sample_statement.pdf) | 11207 ±6105 | 1789 | 9416 | 1 | 0.84 |
| holdings (xlsx) | 2159 ±77 | 726 | 1432 | 1 | 0.66 |
| invest (csv) | 2612 ±1007 | 694 | 1917 | 1 | 0.73 |

## Findings that shape the frontend model

- **Only `post_processing` is observed over SSE.** `preprocessing` / `building_context`
  fire before the client's `EventSource` subscribes and are dropped by the broadcast
  channel, so the reliable checkpoints are `llm_start → llm_progress → post_processing → done`.
- **Pre-LLM** is quasi-static by input *type*: ~800ms for CSV/XLSX, ~1800ms for PDF
  (text extraction). Mildly proportional, low variance.
- **LLM call dominates and is the variance source** (sd up to ±5.6s). It scales with
  input + output size: ~1.4s for a small CSV, ~4.4s for three CSVs, ~9.4s for a PDF.
- **Post-processing (dedup) is effectively instant** (~1ms) at these sizes — folded into
  the final jump to 100%, not its own segment.

## Derived frontend constants (see `frontend/src/pages/import/parse_progress.ts`)

- Segments: **pre [0, 15]**, **llm [15, 90]**, **finalize → 100**.
- Expected durations (drive the per-segment easing time-constant τ):
  `preMs = hasPdf ? 1800 : 800`; `llmMs = hasPdf ? 9000 : 1400 + (n-1)*1500`.
- Within a segment the bar eases toward the ceiling as `v += (ceil - v)·(1 - e^(-dt/τ))`,
  so it slows near the ceiling and never reaches it until the next real event arrives.
- `llm_start`, `post_processing`, `done` snap the bar forward; the resolved POST promise
  forces 100% even if the SSE `done` was missed.
